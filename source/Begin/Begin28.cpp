#include "pt4.h"
#include <cmath>

void Solve()
{
    Task("Begin28");
    double a;
    pt >> a;
    pt << pow(a, 2);
    pt << pow(a, 3);
    pt << pow(a, 5);
    pt << pow(a, 10);
    pt << pow(a, 15);
}
